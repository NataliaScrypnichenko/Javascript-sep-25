// Функції в об’єктах та контекст this
// 2. this (найважливіша різниця)
//
// Звичайна функція має власний this, який залежить від того, як функцію викликають.
//
// Стрілочна функція не має власного this — вона наслідує this із оточення, у якому була створена.
const user = {
    name: "Natalia",
    showName: function() {
        console.log(this.name);
    }
};

user.showName(); // 👉 "Natalia"

//А тепер — стрілкова:
const user = {
    name: "Natalia",
    showName: () => {
        console.log(this.name);
    }
};

user.showName(); // 👉 undefined (або нічого)
//Чому?
// У стрілкової функції немає власного this, тому вона шукає його "зовні" — у глобальному
// контексті (де this не вказує на user).
//Підсумок:
// Якщо треба свій this → використовуй звичайну функцію.
// Якщо треба коротко і без нового контексту → використовуй стрілкову функцію.

// функціїї м
